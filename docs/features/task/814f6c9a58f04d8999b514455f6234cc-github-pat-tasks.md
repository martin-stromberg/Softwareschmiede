# Tasks: GitHub-PAT – Token-Sicherheit und Separation

| # | Bereich | Aufgabe | Status | Testnachweis |
|---|---------|---------|--------|--------------|
| 1 | Logik | `GetGitEnvironment()` erweitern um `GH_TOKEN` Umgebungsvariable (Token aus `ICredentialStore` abrufen und ins Environment-Dictionary einfügen) | Offen | — |
| 2 | Logik | `CloneRepositoryAsync()` anpassen: `BuildAuthenticatedCloneUrl()`-Aufruf entfernen, unauthentifizierte URL verwenden, `GetGitEnvironment()` aufrufen | Offen | — |
| 3 | Logik | `ConfigureGitCredentialsAsync()` anpassen: `git remote set-url` mit unauthentifizierter URL setzen, Token-Embedding entfernen | Offen | — |
| 4 | Logik | `EnsureRemoteCredentialsAsync()` zu Prüfungs-/Normalisierungsfunktion umbauen: Remote-URL normalisieren, eingebettete Tokens entfernen | Offen | — |
| 5 | Test (Unit) | Test `CloneRepositoryAsync_ShouldCallGitClone_WhenCalled()` anpassen auf unauthentifizierte URL und `GH_TOKEN` Verifikation | Offen | — |
| 6 | Test (Unit) | Test `PushBranchAsync_ShouldConfigureRemoteUrlWithToken_BeforePush()` anpassen auf unauthentifizierte Remote-URL und `GH_TOKEN` Verifikation | Offen | — |
| 7 | Test (Unit) | Test `PullAsync_ShouldRunGitPull_WithConfiguredCredentials()` anpassen auf `GH_TOKEN` Umgebungsvariable | Offen | — |
| 8 | Test (Unit) | Neuer Test: `CloneRepositoryAsync_ShouldNotEmbedToken_InCloneUrl()` — Token ist nicht in Clone-URL enthalten | Offen | — |
| 9 | Test (Unit) | Neuer Test: `CloneRepositoryAsync_ShouldPassGhTokenEnvironmentVariable_ForAuthentication()` — `GH_TOKEN` wird übergeben | Offen | — |
| 10 | Test (Unit) | Neuer Test: `EnsureRemoteCredentialsAsync_ShouldRemoveEmbeddedToken_FromRemoteUrl()` — eingebetteter Token wird entfernt | Offen | — |
| 11 | Test (Unit) | Neuer Test: `EnsureRemoteCredentialsAsync_ShouldNormalizeRemoteUrl_ToUnauthenticatedForm()` — URL wird normalisiert | Offen | — |
| 12 | Test (Unit) | Neuer Test: `PushBranchAsync_ShouldNotEmbedToken_InRemoteUrl()` — keine Token in Remote-URL | Offen | — |
| 13 | Test (Unit) | Neuer Test: `PushBranchAsync_ShouldPassGhTokenEnvironmentVariable_ForAuthentication()` — `GH_TOKEN` wird übergeben | Offen | — |
| 14 | Test (Unit) | Neuer Test: `PullAsync_ShouldNotRequireEmbeddedToken_InRemoteUrl()` — Pull funktioniert ohne URL-Embedding | Offen | — |
| 15 | Test (Unit) | Neuer Test: `SanitizeSensitiveOutput_ShouldMaskToken_InPushErrorMessages()` — Token in Fehler-Output maskiert | Offen | — |
| 16 | Test (Unit) | Neuer Test: `SanitizeSensitiveOutput_ShouldMaskToken_InPullErrorMessages()` — Token in Fehler-Output maskiert | Offen | — |
| 17 | Test (E2E) | E2E-Test: Repository-Klonen über UI — Token nicht in `.git/config` oder CLI-Logs | Offen | — |
| 18 | Test (E2E) | E2E-Test: Push nach Klonen — funktioniert mit `GH_TOKEN`, Token nicht in Remote-URL | Offen | — |
| 19 | Test (E2E) | E2E-Test: Pull nach Klonen — funktioniert mit `GH_TOKEN`, Token nicht in Remote-URL | Offen | — |
| 20 | Test (E2E) | E2E-Test: Authentifizierungsfehler-Meldung — Token ist maskiert, nicht im Klartext | Offen | — |
| 21 | Verifikation | Build: `dotnet build Softwareschmiede.slnx` erfolgt fehlerfrei | Offen | — |
| 22 | Verifikation | Tests: `dotnet test src/Softwareschmiede.Tests/Softwareschmiede.Tests.csproj --filter "Category!=OsInterface"` — alle Tests bestanden | Offen | — |
