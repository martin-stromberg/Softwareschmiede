# Tasks: Prereleases bei Updates

Plan: [plan.md](5c1a753c335f441294221c1e260a87c3-prereleases/plan.md). Review: [review.md](5c1a753c335f441294221c1e260a87c3-prereleases/review.md).

Statuslegende: `Erledigt` = Implementierung und vorgesehene Testabdeckung wurden im Quellcode nachgewiesen. `Teilweise` = Implementierung vorhanden, aber ein geforderter Teilnachweis fehlt. `Offen` = nicht nachweisbar bzw. Ausfuehrung explizit nicht erfolgt.

Hinweis zur Verifikation: Bei dieser Review wurden auf Weisung keine Builds oder Tests ausgefuehrt. `Erledigt` bei Test-Tasks bedeutet daher "Testcode vorhanden und fachlich passend", nicht "Testlauf nachgewiesen". Die Ausfuehrungsnachweise bleiben Aufgabe 68/69.

| # | Bereich | Aufgabe | Status | Testnachweis |
|---|---|---|---|---|
| 1 | Datenmodell | `UpdateMode` mit festen Werten 0, 1, 2 anlegen. | Erledigt | `UpdateModels.cs` (`Aus = 0`, `NurPruefen = 1`, `BeiProgrammstartPruefenUndAusfuehren = 2`); `SettingsViewModelTests.UpdateSettings_SaveLoadAllValues` (alle drei Modi), `AppEinstellungServiceTests_UpdateSettings.SetUpdateSettingsAsync_RejectsInvalidUpdateMode`. |
| 2 | Datenmodell | Unveraenderliches `UpdateSettings` fuer das gespeicherte Wertepaar anlegen. | Erledigt | `UpdateModels.cs` (`public sealed record UpdateSettings(UpdateMode Modus, bool IncludePrereleases)`); Roundtrip ueber `UpdateSettings_PersistsAcrossScopes` / `UpdateSettings_SaveLoadAllValues`. |
| 3 | Datenmodell | Unveraenderliches `UpdateCheckOptions` fuer Service-/Releaseaufrufe anlegen. | Erledigt | `UpdateModels.cs` (`public sealed record UpdateCheckOptions(bool IncludePrereleases)`); Signaturverwendung in `UpdateInterfaces.cs` und `UpdateServiceTests_Options`. |
| 4 | Datenmodell | `UpdateInfo.IsPrerelease` und betroffene Konstruktorverwendungen ergaenzen. | Erledigt | `UpdateModels.cs` (`UpdateInfo` mit `bool IsPrerelease`); `GitHubReleaseClient.TryCreateCandidate` setzt Flag aus GitHub-`prerelease` ODER SemVer-Suffix; `GitHubReleaseClientTests_Filters` pruefen `isPrerelease` je Fall. |
| 5 | Persistenz | Einstellungsschluessel und gemeinsame `GetUpdateSettingsAsync`-Lesemethode mit Defaults einfuehren. | Erledigt | `AppEinstellungService.cs` (`UpdateModeKey`/`IncludePrereleasesKey`, Abfragetag `UpdateSettings.Read`, eine Abfrage, Defaults `NurPruefen`/`false` bei fehlenden/ungueltigen Werten, Lesefehler propagieren); `GetUpdateSettingsAsync_ReturnsDefaults_WhenStoredValuesAreMissingOrInvalid`, `GetUpdateSettingsAsync_ReadFailureDoesNotReturnDefaults`. |
| 6 | Persistenz | `SetUpdateSettingsAsync` mit Enum-Validierung und gemeinsamem Speichern beider Werte einfuehren. | Erledigt | `AppEinstellungService.SetUpdateSettingsAsync` (`Enum.IsDefined`-Validierung, beide Keys in einem `SaveChangesAsync`); `SetUpdateSettingsAsync_RejectsInvalidUpdateMode`, `UpdateSettings_SaveFailureLeavesNoMixedPair`, `UpdateSettings_PersistsAcrossScopes`. |
| 7 | UI | Modus-/Checkbox-Properties und feste Auswahlliste in `SettingsViewModel` anlegen. | Erledigt | `SettingsViewModel.cs` (`SelectedUpdateMode`, `IncludePrereleases`, `UpdateModusOptionen` mit exakt drei Labels); `UpdateSettings_SaveLoadAllValues`. |
| 8 | UI | Laden, Speichern und Verwerfen der neuen Einstellungen anbinden. | Erledigt | `SettingsViewModel.LadenAsync`/`SpeichernAsync`/`VerwerfenAsync` (Label-Parsing, `SetUpdateSettingsAsync`, Reload beim Verwerfen); `UpdateSettings_DefaultsAndInvalidValues`, `UpdateSettings_SaveLoadAllValues`, `UpdateSettings_DiscardDoesNotPublish`. |
| 9 | UI | `UpdateSettingsSaved` nach erfolgreichem Speichern des Wertepaars ausloesen. | Erledigt | `SettingsViewModel.UpdateSettingsSaved` (Event direkt nach `SetUpdateSettingsAsync` mit gespeichertem Snapshot); `UpdateSettings_EventFiresBeforeLaterSettingsFailure` (Event auch bei spaeterem Fremdfehler), `UpdateSettings_DiscardDoesNotPublish`. |
| 10 | UI | ComboBox mit exakt drei Labels und Automation-Name in `SettingsView.xaml` einfuegen. | Erledigt | `SettingsView.xaml` (ComboBox `AutomationProperties.Name="Update-Modus"`, `ItemsSource=UpdateModusOptionen`); E-01 `Settings_AllModesAndPrereleasesPersist` prueft alle drei Labels und Lesbarkeit bei minimaler Fenstergroesse. |
| 11 | UI | Prerelease-Checkbox mit Automation-Name in `SettingsView.xaml` einfuegen. | Erledigt | `SettingsView.xaml` (CheckBox `AutomationProperties.Name="Prerelease-Versionen laden"`, TwoWay-Bindung); E-01/E-03. |
| 12 | UI | `MainWindowViewModel` einmalig mit Speichern-Event verbinden und in `Dispose` abmelden. | Erledigt | `MainWindowViewModel.NavigateToSettings` abonniert `UpdateSettingsSaved` einmalig am gecachten `SettingsViewModel`; `Dispose` meldet ab und cancelt den Ablauf; Abdeckung ueber `MainWindowViewModelTests_UpdateStartup` (UI-Speicherpfad `SpeichereUpdateEinstellungenUeberUiAsync`) und E-04. |
| 13 | UI | Command-Zustaende, Angebotsinvalidierung und sichtbaren `UpdateHinweis` im Hauptfenster umsetzen. | Erledigt | `MainWindowViewModel` (`KannUpdatePruefen`/`KannUpdateStarten` verlangen geladene Settings + Modus != `Aus`; `ApplyGespeicherteUpdateSettings` invalidiert Angebot/Generation); `MainWindow.xaml` (Button-Sichtbarkeit/-Bindungen, `UpdateHinweis` mit `AutomationId`); `Aus_BlocksBothCommandsAndDirectInvocation`, `SettingsChange_DiscardsDelayedResultAndStopsPreparation`. |
| 14 | Versionslogik | `SemanticUpdateVersion` mit SemVer-Identifiern und Praezedenz anlegen. | Erledigt | `SemanticUpdateVersion.cs` (X.Y.Z-Kern, Prerelease-Identifier, Metadaten, volle SemVer-Praezedenz); `UpdateVersionComparerTests_SemVer` (Praezedenz, Identifier-Regeln, Metadaten ohne Einfluss). |
| 15 | Versionslogik | `TryParse`, `Normalize`, `IsNewer` auf suffixerhaltenden Vertrag umstellen. | Erledigt | `UpdateVersionComparer.cs` (`TryParse` -> `SemanticUpdateVersion`, `Normalize` wirft `FormatException` und erhaelt Suffix/Metadaten, `IsNewer` semantisch); `Normalize_PreservesPrereleaseAndMetadata`, `Normalize_ThrowsFormatException_WhenVersionIsInvalid`, `TryParse_RejectsInvalidIdentifiers`, `IsNewer_ShouldCompareSemVerValues`. |
| 16 | Versionslogik | Normalisierungsverbraucher einschliesslich `ApplicationVersionProvider`, Versionsanzeige und Paketpfade abgleichen. | Erledigt | `ApplicationVersionProvider` liefert suffixerhaltende Version (`GetInstalledVersionAsync_PreservesPrerelease`); `GitHubReleaseClient` nutzt `Normalize` fuer `UpdateInfo.Version`; `UpdatePackageService` nutzt `update.Version` fuer `extracted/<version>`; Anzeige via `VerfuegbaresUpdate.Version`/Tooltip; Kette durch `CheckForUpdateAsync_FromInstalledPrerelease` und E-03 (`AssertiereInstallationsNachweis` auf `1.4.0-rc.1`-Pfad). |
| 17 | Releasequelle | `IUpdateReleaseClient.GetLatestReleaseAsync` auf explizite Optionen umstellen. | Erledigt | `UpdateInterfaces.cs` (`GetLatestReleaseAsync(UpdateCheckOptions, CancellationToken)`); Implementierung `GitHubReleaseClient`, Aufrufer `UpdateService`; alle Mocks/Handler in den Tests auf die Signatur umgestellt. |
| 18 | Releasequelle | Paginierte Releases-Abfrage mit Link-Verfolgung und Gesamt-Timeout implementieren. | Erledigt | `GitHubReleaseClient.GetLatestReleaseAsync` (`releases?per_page=100`, `Link`/`rel=next`-Auswertung, besuchte URLs, `CancelAfter(_options.CheckTimeout)` ueber alle Seiten); `GetLatestReleaseAsync_SelectsHighestAcrossPages`. |
| 19 | Releasequelle | Draft-, Tag-, Kanal- und Assetfilter samt hoechster Version ueber alle Seiten implementieren. | Erledigt | `GitHubReleaseClient.TryCreateCandidate` (Draft, leerer/ungueltiger Tag, Kanal, Asset-Name + absolute HTTP(S)-URL; Maximum per `SemanticUpdateVersion.CompareTo` seitenuebergreifend); `GetLatestReleaseAsync_FiltersByOptions`, `_FiltersGitHubPrereleaseFlag`, `_FiltersSemVerPrereleaseSuffix`, `_SkipsDraftInvalidTagAndUnusableAssetEntries`, `_SelectsHighestAcrossPages`. |
| 20 | Releasequelle | Folgeseitenfehler ohne Teiltreffer und propagierte Caller-Cancellation implementieren. | Erledigt | `GitHubReleaseClient` (HTTP-/JSON-/Timeout-Fehler und ungueltige/unerlaubte Folge-URL -> `null`; `OperationCanceledException` bei Caller-Abbruch wird weitergeworfen); `GetLatestReleaseAsync_LaterPageFailureDiscardsCandidate`, `_LaterPageJsonFailureDiscardsCandidate`, `_LaterPageTimeoutDiscardsCandidate`, `_RejectsPaginationCycle`, `_RejectsDisallowedFollowUpUrls`, `_PropagatesCallerCancellation`; Serviceebene `UpdateServiceTests_PrereleaseChain.GetLatestReleaseAsync_LaterPageFailureDiscardsCandidate`. |
| 21 | Update-Service | `IUpdateService.CheckForUpdateAsync` und alle Aufrufer/Mocks auf Optionen umstellen. | Erledigt | `UpdateInterfaces.cs`, `UpdateService.CheckForUpdateAsync(UpdateCheckOptions, ct)`; Aufrufer in `MainWindowViewModel` (Start, Pruefen, Starten) und `ProtokollierenderUpdateService` reichen Optionen durch; saemtliche Testmocks auf `UpdateCheckOptions` umgestellt. |
| 22 | Update-Service | Optionsweitergabe und defensiven Prerelease-Ausschluss im `UpdateService` implementieren. | Erledigt | `UpdateService.cs` (Optionen an den Releaseclient; `latest.IsPrerelease && !options.IncludePrereleases` -> `NichtPruefbar`); `CheckForUpdateAsync_PropagatesCurrentOptions`, `CheckForUpdateAsync_ExcludesPrereleaseResult_WhenOptionsDisallowPrereleases`. |
| 23 | Update-Service | Verbraucher von `CachedResult` ermitteln und Cache gemaess Plan entfernen oder optionsgebunden invalidieren. | Erledigt | Kein `CachedResult`/`_cachedResult` mehr unter `src/`; `UpdateService` enthaelt keinen Ergebnis-Cache (jede Pruefung geht durch `_checkGate` zum Client). Optionswechsel `true -> false` ohne altes RC-Ergebnis durch `CheckForUpdateAsync_PropagatesCurrentOptions` abgesichert. |
| 24 | Startfluss | Konstruktorpruefung entfernen und `InitializeUpdatesAfterWindowReadyAsync` mit einmaligem Startkennzeichen implementieren. | Erledigt | `MainWindowViewModel.InitializeUpdatesAfterWindowReadyAsync` (`Interlocked.Exchange` auf `_startInitialisierungErfolgt` vor erstem `await`); `Constructor_DoesNotCheckUpdates`, `WindowReady_StartsOnceWithImmediateResult`, erneuter Bereitschaftsaufruf in `Startup_ConcurrentCommandsCannotDuplicateInstall`. |
| 25 | Startfluss | `Application.MainWindow` vor `Show` setzen und einmaligen `ContentRendered`-Handler verdrahten. | Erledigt | `App.xaml.cs` (`Application.Current.MainWindow = mainWindow` vor `mainWindow.Show()`); `MainWindow.xaml.cs` (`ContentRendered += OnContentRendered`, Handler meldet sich selbst ab und ruft die Bereitschaftsmethode beobachtet via `SafeFireAndForget`); Owner-Nachweis in E-02 (`GW_OWNER`). |
| 26 | Startfluss | Einstellungen pro Vorgang ueber frischen Scope laden und alle Eintrittspunkte bei `Aus` sperren. | Erledigt | `LeseUpdateSettingsAsync` oeffnet je Aufruf einen DI-Scope und liest `GetUpdateSettingsAsync`; `Aus`-Abbruch in Start, Pruefen und Starten vor jedem Releaseabruf; `Aus_BlocksBothCommandsAndDirectInvocation`, E-02/E-04 (Nullabruf). |
| 27 | Startfluss | Gemeinsames nicht wartendes Gate fuer Pruefung und komplette Installation implementieren. | Erledigt | `_updateGate` (`SemaphoreSlim`, `WaitAsync(0)`) um Start, `UpdatePruefenAsync` und `UpdateStartenAsync` inklusive `InstalliereUpdateAsync`; `Startup_ConcurrentCommandsCannotDuplicateInstall`, E-07. |
| 28 | Startfluss | Einstellungsgeneration, Cancellation und Aktualitaetspruefungen vor Vorbereitung/Start implementieren. | Erledigt | Generation (`_updateSettingsGeneration`, `IstUpdateVersuchAktuell`), verknuepfter `_updateAblaufCts`, erneutes Lesen/Vergleichen der gespeicherten Werte vor `PrepareUpdateAsync` und vor `StartPreparedUpdateAsync`; `SettingsChange_DiscardsDelayedResultAndStopsPreparation`, `UpdateSettingsReadFailure_BeforePreparationStopsInstall`/`_BeforeUpdaterStartStopsInstall`. |
| 29 | Installation | Gemeinsamen manuellen/automatischen Installationskern mit CLI-Sicherheit und echtem Fortschritt extrahieren. | Erledigt | `InstalliereUpdateAsync` wird von Startautomatik und manuellem Start genutzt; `ICliUpdateSafetyService.CheckAsync` + `BestaetigenDialog` ("Update starten?"), `UpdateProgressViewModel` + `IUpdateProgressDialogService` mit echtem Fortschritt; `Startup_SafetyDeclined_DoesNotContinue` (beide Pfade), E-06 (echte Ja/Nein-Dialoge). |
| 30 | Installation | Fehler-/Abbruchzustaende, Gate-Freigabe und ausschliesslichen Shutdown nach Starterfolg erhalten. | Erledigt | `InstalliereUpdateAsync` (Abbruch-/Fehlerzustand im Dialog, `finally` gibt Gate/Busy frei); `UpdateService.StartPreparedUpdateAsync` ruft `Shutdown` erst nach erfolgreichem `StartScriptAsync`; `Startup_SafetyCancelled_DoesNotContinue`, `Startup_PrepareOrStartFailure_DoesNotShutdown`, `StartPreparedUpdateAsync_ShouldNotShutdown_WhenScriptStartFails`. |
| 31 | Testinfrastruktur | `UpdateE2ETestConfiguration` und explizit aktivierte App-DI-Registrierung anlegen. | Erledigt | `UpdateE2ETestConfiguration` (`SOFTWARESCHMIEDE_UPDATE_TEST_CONFIG` + `SOFTWARESCHMIEDE_TEST_DB_PATH` beide erforderlich, Diagnose statt Fallback); `App.ConfigureServices` registriert Testadapter nur bei aktivem Kontext (letzte Registrierung gewinnt). |
| 32 | Testinfrastruktur | `UpdateFixtureHttpMessageHandler` mit JSON/ZIP, Link-Headern, Fehlern und Stream-Freigaben anlegen. | Erledigt | `UpdateFixtureHttpMessageHandler` (Szenario pro Request, Antwort-/Stream-Gates `GateBlockierterStream`, Fehlerfaelle, Wurzel-Pfadpruefung); `UpdateFixtureHttpMessageHandlerTests` (7 Tests inkl. unbekannter URL ohne Netzwerkfallback). |
| 33 | Testinfrastruktur | `RecordingUpdateProcessLauncher` ohne realen Prozessstart anlegen. | Erledigt | `RecordingUpdateProcessLauncher` protokolliert Skriptpfad, Arbeitsverzeichnis, Argumente, Elevation und steuerbares Ergebnis; Nachweis in `AssertiereInstallationsNachweis` und Launcherfehler-Variante (E-06d, `Ergebnis = "Fehler"`). |
| 34 | Testinfrastruktur | `RecordingApplicationShutdownService` mit protokolliertem Shutdown-Aufruf anlegen. | Erledigt | `RecordingApplicationShutdownService` schreibt `ShutdownRequested`, App bleibt offen; Zaehl-/Reihenfolge-Assertions in E-02/E-06/E-07 und Fixture-Smoke. |
| 35 | Testinfrastruktur | `FixtureCliUpdateSafetyService` fuer echte Sicherheitsdialoge ohne ConPTY anlegen. | Erledigt | `FixtureCliUpdateSafetyService` (szenariogesteuerte riskante Aufgaben, `CliSafetyChecked`-Ereignis); echte Win32-Ja/Nein-Bedienung via `UpdateSafetyDialogView` in E-06(a/b). |
| 36 | Testinfrastruktur | `UpdateE2EFixture` mit Testwurzel, lokaler Version, SQLite-DB und unterscheidbaren Release-ZIPs anlegen. | Erledigt | `UpdateE2EFixture` (eigene Tempwurzel mit `installed/version.json` 1.2.0, `test.db`, `scenario.json`, Antwort-/ZIP-Dateien, `events.jsonl`; Stable `1.3.0`/RC `1.4.0-rc.1` mit eigenen URLs und Markern `stable-marker`/`rc-marker`). |
| 37 | Testinfrastruktur | JSONL-Ereignisprotokoll, Abschlussmarker und phasenbezogene Assertions bereitstellen. | Erledigt | `UpdateE2EProtokoll`/`UpdateE2EEreignisse` (u. a. `DatabaseInitializationCompleted`, `WindowReady`, `UpdateAttempt*`, `HttpRequest*`, `UpdateCheck*`, `Preparation*`, `UpdateStart*`, `UpdateProcessStartRecorded`, `ShutdownRequested`, `UpdateSettingsRead*`, `CliSafetyChecked`, `UnknownHttpRequest`); Phasenhelfer `ProtokollBasis`/`EintraegeSeit`/`ZaehleEreignisSeit`/`AssertSequenz` in `E2E_UpdateSettings.cs`. |
| 38 | Testinfrastruktur | `WpfTestBase` um Launch-Konfiguration und `RestartAppPreservingDatabase` mit neuen UIA-Handles erweitern. | Erledigt | `WpfTestBase.LaunchApp` (prozessbezogene Umgebungsvariablen, DB-Pfad-Override, App-Log-Snapshot/Startup-Diagnose) und `RestartAppPreservingDatabase` (regulaeres Schliessen, Exit-Abwarten, neue UIA-Handles, `ensureDatabaseDeleted: false`); Verwendung in allen E-0x-Szenarien. |
| 39 | Testinfrastruktur | Settings-FlaUI-View um Modus-/Checkbox-Getter/Setter und Speichern-/Reload-Warten erweitern. | Erledigt | `Views/SettingsView.cs` (`GetUpdateMode`/`SetUpdateMode`/`WaitForUpdateMode`, `GetIncludePrereleases`/`SetIncludePrereleases`, `SaveSettings`/`WaitForSettingsSaved`, `DiscardChanges`). |
| 40 | Testinfrastruktur | Fortschritts-FlaUI-View um Phase, Fortschritt, Abbrechen, Fehler und Schliessen erweitern. | Erledigt | `Views/Dialogs/UpdateProgressDialogView.cs` (`GetPhase`, `GetMessage`, `GetProgress`, `Cancel`/`CanCancel`, `WaitForErrorState`, `Close`). |
| 41 | Testinfrastruktur | Hauptfenster-/Menue-/Sicherheitsdialoghelfer fuer Updateangebot und Command-Zustaende erweitern. | Erledigt | `Views/MenuView.cs` (`ClickUpdatePruefen`/`ClickUpdateStarten`, `IsUpdateStartButtonVisible`/`Enabled`, `IsUpdatePruefenButtonEnabled`, `GetOfferedUpdateVersion`/`WaitForOfferedUpdateVersion`, `GetUpdateHinweis`/`WaitForUpdateHinweis`); `Views/Dialogs/UpdateSafetyDialogView.cs` (`Confirm`/`Cancel` ueber IDYES/IDNO). |
| 42 | Testinfrastruktur | Fixture-Smoke fuer echte Updatepipeline, Isolation und unbekannte Requests ohne Fallback erstellen. | Erledigt | `E2E_UpdateFixture.cs`: `Fixture_UsesIsolatedRealUpdatePipeline` (echte JSON-/ZIP-Kette, Testpfad-Isolation, Launcher-/Shutdown-Grenze, DB-erhaltender Neustart mit RC-Auswahl, `UnknownHttpRequest` ohne Fallback) und `Fixture_SettingsReadFailureTargetsPhaseAfterDatabaseInitialization` (alle drei Lesegrenzen, Freigabe/Abbruch/Timeout, Persistenz des Fehlers bis Deaktivierung). |
| 43 | Tests | Settings-Defaults, alle Wertepaare, Verwerfen und erfolgreiches Speichern-Event testen. | Erledigt | `SettingsViewModelTests`: `UpdateSettings_DefaultsAndInvalidValues`, `UpdateSettings_SaveLoadAllValues` (3 Modi x 2 Checkboxzustaende), `UpdateSettings_DiscardDoesNotPublish`, `UpdateSettings_EventFiresBeforeLaterSettingsFailure`. Ausfuehrungsnachweis: Task 68. |
| 44 | Tests | SQLite-Persistenz ueber neue Scopes und Fehler ohne gemischtes Wertepaar testen. | Erledigt | `AppEinstellungServiceTests_UpdateSettings`: `UpdateSettings_PersistsAcrossScopes`, `UpdateSettings_SaveFailureLeavesNoMixedPair`, `GetUpdateSettingsAsync_ReadFailureDoesNotReturnDefaults`, `GetUpdateSettingsAsync_ReturnsDefaults_WhenStoredValuesAreMissingOrInvalid`, `SetUpdateSettingsAsync_RejectsInvalidUpdateMode`. Ausfuehrungsnachweis: Task 68. |
| 45 | Tests | SemVer-Praezedenz, ungueltige Identifier und suffixerhaltende Normalisierung testen (T-05). | Erledigt | `UpdateVersionComparerTests_SemVer` (`Normalize_PreservesPrereleaseAndMetadata`, `Compare_PrereleasePrecedence`, `Compare_PrereleaseIdentifierRules`, `Compare_MetadataDoesNotAffectPrecedence`, `TryParse_RejectsInvalidIdentifiers`, `TryParse_ExposesPrereleaseIdentifiersAndMetadata`, `Normalize_ThrowsFormatException_WhenVersionIsInvalid`) plus `UpdateVersionComparerTests`. Ausfuehrungsnachweis: Task 68. |
| 46 | Tests | `ApplicationVersionProviderTests` um echte Prerelease-`version.json` ergaenzen (T-05). | Erledigt | `ApplicationVersionProviderTests.GetInstalledVersionAsync_PreservesPrerelease`; Stable- und Fehlerfaelle erhalten (`ShouldReadValidVersionJson`, `ShouldReturnNull_WhenVersionJsonIsInvalid`). Ausfuehrungsnachweis: Task 68. |
| 47 | Tests | Releaseclient-Tag-Normalisierung und komplette Provider-/Client-Folgeupdatekette testen (T-05). | Erledigt | `GitHubReleaseClientTests_Filters.GetLatestReleaseAsync_PreservesPrereleaseVersion`; `UpdateServiceTests_PrereleaseChain.CheckForUpdateAsync_FromInstalledPrerelease` (echter Provider + echter Client: `rc.2`/`1.3.0` neuer als installiertes `1.3.0-rc.1`, `rc.1`/`beta.2`/`1.2.9` nicht; RC nur mit Checkbox). Ausfuehrungsnachweis: Task 68. |
| 48 | Tests | Releasefilter fuer beide Kanaele und Optionswechsel ohne altes Cacheergebnis testen (T-02/T-03). | Erledigt | `GitHubReleaseClientTests_Filters` (`FiltersByOptions`, `FiltersGitHubPrereleaseFlag`, `FiltersSemVerPrereleaseSuffix`, `SkipsDraftInvalidTagAndUnusableAssetEntries`); `UpdateServiceTests_Options.CheckForUpdateAsync_PropagatesCurrentOptions` und `_ExcludesPrereleaseResult_WhenOptionsDisallowPrereleases`. Ausfuehrungsnachweis: Task 68. |
| 49 | Tests | Hoechste Version auf Folgeseite bei unsortierten/ungueltigen Vorgaengern testen (T-06). | Erledigt | `GitHubReleaseClientTests_Pagination.GetLatestReleaseAsync_SelectsHighestAcrossPages` (Theory beider Kanaele, Abruf beider Seiten). Ausfuehrungsnachweis: Task 68. |
| 50 | Tests | HTTP-/JSON-/Timeoutfehler auf Folgeseite, Pagination-Zyklen und Cancellation testen (T-06). | Erledigt | `GitHubReleaseClientTests_Pagination` (`LaterPageFailureDiscardsCandidate`, `LaterPageJsonFailureDiscardsCandidate`, `LaterPageTimeoutDiscardsCandidate`, `RejectsPaginationCycle`, `RejectsDisallowedFollowUpUrls`, `PropagatesCallerCancellation`); zusaetzlich Serviceebene in `UpdateServiceTests_PrereleaseChain`. Ausfuehrungsnachweis: Task 68. |
| 51 | Tests | Konstruktor ohne Pruefung, explizite Bereitschaft und sofortige Startantwort fuer alle Modi testen (T-01). | Erledigt | `MainWindowViewModelTests_UpdateStartup.Constructor_DoesNotCheckUpdates`, `WindowReady_StartsOnceWithImmediateResult` (Theory ueber alle drei Modi, sofortiges Ergebnis, Einmaligkeit). Ausfuehrungsnachweis: Task 68. |
| 52 | Tests | `Aus` fuer beide Commands und direkte Aufrufe sowie veraltete Ergebnisse nach Speichern testen (P-01/T-03). | Erledigt | `MainWindowViewModelTests_UpdateStartup.Aus_BlocksBothCommandsAndDirectInvocation` (Commands gesperrt, direkte Methodenaufrufe ohne Abruf, Wechsel aus aktivem Modus), `SettingsChange_DiscardsDelayedResultAndStopsPreparation` (verzoegerte Antworten/Abbruch-resistente Resultate nach `Aus`/Kanalwechsel verworfen, kein Start/Shutdown). Ausfuehrungsnachweis: Task 68. |
| 53 | Tests | Kein Update und nicht pruefbare lokale/entfernte Quelle ohne Installationsfolgeschritte testen (T-04). | Erledigt | `MainWindowViewModelTests_UpdateStartup.Startup_NoUpdateOrNotCheckable_DoesNotInstall` (Theory), `UpdateServiceTests.CheckForUpdateAsync_ShouldReturnNotCheckable_WhenLocalVersionIsMissing`. Ausfuehrungsnachweis: Task 68. |
| 54 | Tests | Automatische Sicherheitsablehnung, Abbruch, Prepare-/Startfehler ohne Shutdown testen (T-04). | Erledigt | `MainWindowViewModelTests_UpdateStartup.Startup_SafetyDeclined_DoesNotContinue`, `Startup_SafetyCancelled_DoesNotContinue`, `Startup_PrepareOrStartFailure_DoesNotShutdown` (je `automatisch`/`fehlerBeimStart` parametrisiert); `UpdateServiceTests.StartPreparedUpdateAsync_ShouldNotShutdown_WhenScriptStartFails`. Ausfuehrungsnachweis: Task 68. |
| 55 | Tests | Verzoegerte Pruefung/Vorbereitung mit konkurrierenden Commands und wiederholter Bereitschaft testen (T-07). | Erledigt | `MainWindowViewModelTests_UpdateStartup.Startup_ConcurrentCommandsCannotDuplicateInstall` (blockierte Pruefung und Vorbereitung via `TaskCompletionSource`, parallele Commands/Direktaufrufe verworfen, genau ein Check/Prepare/Start, erneuter Bereitschaftsaufruf wirkungslos). Ausfuehrungsnachweis: Task 68. |
| 56 | Regression | Stable-only-Erwartungen, Interface-Mocks und bisherigen Konstruktor-Prueftest gezielt aktualisieren (T-08). | Erledigt | `UpdateVersionComparerTests` angepasst (`IsNewer_ShouldCompareSemVerValues` erwartet nun `1.2.3 -> 1.2.4-beta.1 = true`; `TryParse_ShouldAcceptSemVerTags` akzeptiert `v1.2.3-beta.1`/Metadaten, lehnt Kurz-/ungueltige Tags ab); bisheriger Konstruktor-Prueftest durch `Constructor_DoesNotCheckUpdates` ersetzt; alle `IUpdateReleaseClient`/`IUpdateService`-Mocks auf Optionssignatur und Listen-Responses umgestellt (kein `/latest`-Einzelobjekt mehr). Ausfuehrungsnachweis: Task 68. |
| 57 | Regression | Bestehende manuelle Sicherheitsablehnungs-, Abbruch- und Fortschrittstests erhalten und automatisch spiegeln (T-08). | Erledigt | Manuelle und automatische Pfade gemeinsam parametrisiert: `Startup_SafetyDeclined_DoesNotContinue(automatisch: false/true)`, `Startup_SafetyCancelled_DoesNotContinue(false/true)`, `Startup_PrepareOrStartFailure_DoesNotShutdown`; Dialogzustaende zusaetzlich ueber `UpdateProgressViewModelTests` (u. a. `MarkUpdaterStarting_ShouldEnableCloseAndDisableCancel`, `SetError_ShouldEnableClose`) und `UpdateProgressDialogTests.Show_ShouldNotThrowBindingException`. Ausfuehrungsnachweis: Task 68. |
| 58 | Regression | `UpdatePackageServiceTests` einschliesslich RC-Paketpfad sowie gesamte `UpdateScriptServiceTests` ausfuehren (T-08). | Teilweise | `UpdatePackageServiceTests` deckt Download/Entpacken/Validierung/Pfadbegrenzung/Atomaritaet/Aufraeumen ab; `UpdateScriptServiceTests` vollstaendig vorhanden (4 Tests). Kein dedizierter Unit-Test mit RC-Versionspfad in `UpdatePackageServiceTests` gefunden; der RC-Pfad (`extracted/1.4.0-rc.1` mit passender `version.json`) wird stattdessen end-to-end in E-03/Fixture-Smoke ueber `AssertiereInstallationsNachweis` nachgewiesen. Ausfuehrungsnachweis: Task 68. |
| 59 | Regression | Skriptstartfehler ohne Shutdown, CLI-Sicherheit und Fortschritts-/Dialogregressionen erhalten (T-08). | Erledigt | `UpdateServiceTests.StartPreparedUpdateAsync_ShouldNotShutdown_WhenScriptStartFails`; `CliUpdateSafetyServiceTests` (frischer vs. abgelaufener Heartbeat); `UpdateProgressViewModelTests` (5 Tests); `UpdateProgressDialogTests.Show_ShouldNotThrowBindingException`. Ausfuehrungsnachweis: Task 68. |
| 60 | E2E-Tests | E-01: alle Modus-/Checkboxwerte speichern, verwerfen, erneut anzeigen und nach Neustart pruefen. | Erledigt | `E2E_UpdateSettings.Settings_AllModesAndPrereleasesPersist` (alle drei exakten Labels + beide Checkboxzustaende ueber echte UI, Weg-/Zuruecknavigation, Verwerfen, echte DB-erhaltende Neustarts, Minimal-Fenstergroesse, Protokollnachweis ohne Launcher/Shutdown). Ausfuehrungsnachweis: Task 68/69. |
| 61 | E2E-Tests | E-02: alle drei Modi mit UI-Speichern, echter Startwirkung und negativem Aufrufprotokoll testen (T-01). | Erledigt | `E2E_UpdateSettings.Startup_ModesDriveUpdatePipeline` (`Aus`: null HTTP/Check/Launcher/Shutdown, Pruefen deaktiviert, Installieren verborgen; `Nur Pruefen`: Stable-Angebot ohne Asset/Start/Shutdown; Startmodus: Owner-nachgewiesener Fortschrittsdialog, blockierter Download, nach Freigabe `AssertiereInstallationsNachweis` mit genau einem Launcher-Erfolg vor einem Shutdown). Ausfuehrungsnachweis: Task 68/69. |
| 62 | E2E-Tests | E-03: Stable/RC-Angebot und passendes vorbereitetes Asset manuell und automatisch testen (T-02). | Erledigt | `E2E_UpdateSettings.PrereleaseCheckbox_SelectsMatchingAsset` (manuell Stable/RC je Checkboxzustand mit exklusivem Asset-Abruf und Installationsnachweis; beide Zustaende zusaetzlich im UI-gespeicherten Startmodus nach echtem Neustart). Ausfuehrungsnachweis: Task 68/69. |
| 63 | E2E-Tests | E-04: gespeicherte Kanal-/Aus-Wechsel nach altem RC-Fund und verzogerter Antwort testen (T-03). | Erledigt | `E2E_UpdateSettings.SavedChangesInvalidatePreviousOffer` (RC-Angebot -> Checkbox aus: Angebot weg ohne neuen Request; Stable-Neupruefung/-installation; `Aus` bei blockiertem laufendem Request: spaete Antwort verworfen, kein Download; Neustart bestaetigt Nullabruf). Ausfuehrungsnachweis: Task 68/69. |
| 64 | E2E-Tests | E-05: kein Update und nicht pruefbare Startfaelle mit weiter bedienbarer App testen (T-04). | Erledigt | `E2E_UpdateSettings.Startup_NoUpdateOrUncheckableRemainsUsable` (gleiche/aeltere Version, ungueltige und fehlende lokale `version.json` ohne Releaseabruf, Release-HTTP-500 und defektes Release-JSON -> `NichtPruefbar`-Hinweis; je Fall kein Angebot/Dialog/Asset/Launcher/Shutdown, Einstellungen bedienbar). Ausfuehrungsnachweis: Task 68/69. |
| 65 | E2E-Tests | E-06: echte Sicherheitsdialoge, Fortschrittsabbruch und sichtbare Prepare-/Startfehler testen (T-04). | Erledigt | `E2E_UpdateSettings.Startup_SafetyCancelAndErrorsRemainUsable` ((a) Nein-Ablehnung, (b) Ja + blockierter Download + Abbrechen, (c) Asset-HTTP-Fehler automatisch + defektes ZIP manuell, (d) Launcherfehler ohne Shutdown; danach alle drei T-09-Lesefehler-Grenzen automatisch und manuell inkl. Gate-Freigabe, null Folgeschritten und erfolgreicher Folgepruefung nach Deaktivierung). Ausfuehrungsnachweis: Task 68/69. |
| 66 | E2E-Tests | E-07: gesperrte Commands und einmalige automatische Vorbereitung/Uebergabe testen (T-07). | Erledigt | `E2E_UpdateSettings.Startup_IsOnceAndCommandsStayBlocked` (blockierter ZIP-Stream: Commands gesperrt, versuchte UI-Aktivierung ohne zweiten Ablauf/Shutdown; nach Freigabe genau ein Versuch/Vorbereitung/Launcher/Shutdown; Navigation und erneutes Rendern im offenen Fenster ohne Zaehleraenderung). Ausfuehrungsnachweis: Task 68/69. |
| 67 | E2E-Tests | Szenarien in `E2E_UpdateSettings.cs` und dauerhaft konsolidiert in `MainTest.RunGeneralTests` integrieren. | Erledigt | `E2E_UpdateSettings.cs` als Partial von `End2EndTest`; `MainTest.RunGeneralTests` ruft beide Fixture-Smokes und alle sieben Szenarien sequentiell; `RunConPtyTests` bleibt separat. Ausfuehrungsnachweis: Task 68/69. |
| 68 | Verifikation | Vollen Build und getrennte regulaere/OS-Testspuren nach `CLAUDE.md` ausfuehren. | Offen | Nicht ausgefuehrt: Diese Review durfte per Vorgabe keine Builds/Tests starten. Es liegt kein Ausfuehrungsnachweis (Build, `Category!=OsInterface`, `Category=OsInterface` mit `SOFTWARESCHMIEDE_SKIP_CONPTY_TESTS=1`) vor. |
| 69 | Verifikation | Ergebnisse aller E2E-Varianten und Regressionen einschliesslich fehlender Nachweise in `test-results.md` dokumentieren. | Offen | Kein `test-results.md` im Feature-Verzeichnis vorhanden; ohne Testlaeufe (Task 68) kann die geforderte Ergebnisdokumentation nicht erstellt werden. |
