# Tasks: Issue 174 — Plugins-Einstellungen UI-Korrekturen

| # | Bereich | Aufgabe | Status | Testnachweis |
|---|---------|---------|--------|--------------|
| 1 | UI (Kontrast) | Beiden Aktivierungs-`ListBox`en in `SettingsView.xaml` themed `Background="{DynamicResource SurfaceBrush}"` und `Foreground="{DynamicResource PrimaryTextBrush}"` geben | Erledigt | Kein direkter Test (rein visuell); vorhanden in `SettingsView.xaml` Zeilen 286-287 (SCM) und 345-346 (KI) |
| 2 | UI (Kontrast) | `ItemContainerStyle` beider `ListBox`en von reinem `EventSetter` auf `ListBoxItem`-Style mit themed Hintergrund + `IsSelected`/`IsMouseOver`-Triggern umstellen | Erledigt | Kein direkter Test; `ListBoxItem`-Style mit Triggern in `SettingsView.xaml` Zeilen 291-325 (SCM) und 349-383 (KI) |
| 3 | UI (Liste) | Aktivierungs-`CheckBox` aus dem `ItemTemplate` der SCM-`ListBox` entfernen; Namens-`TextBlock` inkl. `AutomationProperties.Name="{...}.Eintrag"` beibehalten | Erledigt | `E2E_PluginAktivierung` (Selektion über `ByName("LocalDirectoryPlugin.Eintrag")`; würde bei fehlendem `.Eintrag`-Namen fehlschlagen) |
| 4 | UI (Liste) | Aktivierungs-`CheckBox` aus dem `ItemTemplate` der KI-`ListBox` entfernen; Namens-`TextBlock` inkl. `AutomationProperties.Name="{...}.Eintrag"` beibehalten | Erledigt | `E2E_PluginAktivierung` (`DeaktivierePlugin` selektiert über `ByName("{prefix}.Eintrag")`) |
| 5 | UI (rechter Bereich) | `ScrollViewer Grid.Column="2"`-Inhalt in ein `StackPanel` umbauen (Kopfzeile + CheckBox + bestehendes `ItemsControl`) | Erledigt | `E2E_PluginAktivierung` (findet `PluginAktiviert`-CheckBox neben den Plugin-Settings); `SettingsView.xaml` Zeilen 401-443 |
| 6 | UI (rechter Bereich) | Kopfzeilen-`TextBlock` gebunden an `SelectedPlugin.PluginName` (Bold, `PrimaryTextBrush`) einfügen | Erledigt | Kein direkter Test; `SettingsView.xaml` Zeilen 402-406 |
| 7 | UI (rechter Bereich) | Aktivierungs-`CheckBox` „Plugin aktiviert" mit `AutomationProperties.Name="PluginAktiviert"` und `IsChecked="{Binding SelectedPlugin.IsEnabled, Mode=TwoWay}"` einfügen | Erledigt | `E2E_PluginAktivierung` (schaltet `ByName("PluginAktiviert")` um und prüft `IsChecked`) |
| 8 | UI (Sichtbarkeit) | Kopfzeile + CheckBox über `NullOrEmptyToVisibilityConverter` an `SelectedPlugin` sichtbar schalten (null → Collapsed) | Erledigt | Kein direkter Test; `SettingsView.xaml` Zeilen 405 und 410 |
| 9 | Code-Behind | Handler `OnPluginActivationItemPreviewMouseLeftButtonDown` in `SettingsView.xaml.cs` entfernen | Erledigt | Kein direkter Test; per Grep in `src/` nicht mehr vorhanden |
| 10 | E2E-Tests | `E2E_PluginAktivierung.DeaktivierePlugin`-Helper auf „Item selektieren (`{prefix}.Eintrag`) → CheckBox `PluginAktiviert` umschalten" umstellen | Erledigt | `E2E_PluginAktivierung.DeaktivierePlugin` (Zeilen 145-151) |
| 11 | E2E-Tests | SCM-Validierungsphase in `E2E_PluginAktivierung` auf neuen Ablauf umstellen (Item selektieren, rechte CheckBox deaktivieren, Fehlermeldung prüfen) | Erledigt | `E2E_PluginAktivierung.DeaktivierenDesLetztenScmPlugins_ZeigtValidierungsfehler_E2E` |
| 12 | E2E-Tests | Reload-Assertion in `E2E_PluginAktivierung` auf „ClaudeCli-Item selektieren → CheckBox `PluginAktiviert` IsChecked == false" umstellen | Erledigt | `E2E_PluginAktivierung.DeaktivierenVonDreiKiPlugins_PersistiertUndBlendetAuswahlAus_E2E` (Zeilen 112-115) |
| 13 | Verifikation | Voller Build + reguläre Unit-Tests (`SettingsViewModelTests`, `PluginActivationServiceTests`) und angepasster E2E-Test (`Category=OsInterface`) ausführen | Erledigt | `dotnet build` fehlerfrei; `--filter "Category!=OsInterface"` 1083 bestanden/1 übersprungen; `--filter "Category=OsInterface"` mit `SOFTWARESCHMIEDE_SKIP_CONPTY_TESTS=1`: `E2E_PluginAktivierung` bestanden (einziger Fehlschlag ist der umgebungsbedingte, unabhängige Clipboard-Test `TerminalControlTests.OnPreviewKeyDown_CtrlV_SetsHandledTrue`) |
| 14 | Code-Review-Befund (`review-code.md`) | Duplizierten `ListBoxItem`-`ControlTemplate` zwischen SCM- und KI-`ListBox` als einmaligen Style `PluginActivationListBoxItemStyle` in `UserControl.Resources` extrahieren und über `ItemContainerStyle="{StaticResource ...}"` referenzieren | Erledigt | Build fehlerfrei; `SettingsView.xaml` Zeilen 87-119 (Style-Definition), 324 und 347 (Referenzierung) |
| 15 | Code-Review-Befund (`review-code.md`) | Lokalen `Foreground="{DynamicResource PrimaryTextBrush}"` am Namens-`TextBlock` in beiden `ItemTemplate`s entfernen, damit der Trigger-`Foreground="White"` bei `IsMouseOver`/`IsSelected` tatsächlich greift (analog zum `ComboBoxItem`-Referenzmuster in `LightTheme.xaml`/`DarkTheme.xaml`) | Erledigt | `E2E_PluginAktivierung` bestanden; `SettingsView.xaml` Zeilen 329-332 (SCM) und 352-355 (KI) ohne lokalen `TextBlock.Foreground` |
