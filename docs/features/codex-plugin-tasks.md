# Tasks: ConPTY-basiertes Terminal-Control

| # | Bereich | Aufgabe | Status | Testnachweis |
|---|---------|---------|--------|--------------|
| 1 | Infrastruktur | `PseudoConsoleNativeMethods` anlegen (P/Invoke: `CreatePseudoConsole`, `ResizePseudoConsole`, `ClosePseudoConsole`, `CreateProcess`, `InitializeProcThreadAttributeList`, `UpdateProcThreadAttribute`, `DeleteProcThreadAttributeList`, `CreatePipe`, `CloseHandle`) | Erledigt | Kein direkter Test; abgesichert durch `PseudoConsole.Create` (wird in `KiAusfuehrungsService.StartWithPseudoConsoleAsync` getestet) |
| 2 | Infrastruktur | `PseudoConsole` anlegen (IDisposable-Wrapper für `HPCON`, Pipe-Handles; `Create`-Fabrikmethode; `Resize(short cols, short rows)`; `Dispose`) | Erledigt | Abgesichert durch `KiAusfuehrungsService.StartWithPseudoConsoleAsync` (Teil des gesamten ConPTY-Start-Prozesses) |
| 3 | Infrastruktur | `PseudoConsoleProcessStarter` anlegen (`Start(ProcessStartInfo, PseudoConsole)` → Win32-Prozess via `STARTUPINFOEX` + `PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE`) | Erledigt | Abgesichert durch `KiAusfuehrungsService.StartWithPseudoConsoleAsync` |
| 4 | Infrastruktur | `PseudoConsoleSession` anlegen (koordiniert `PseudoConsole`, `Process`, `InputStream`, `OutputStream`; `ResizeAsync`; `Dispose`) | Erledigt | Abgesichert durch `TerminalControl.OnSessionChanged` und `KiAusfuehrungsService.StartWithPseudoConsoleAsync` |
| 5 | Datenmodell | `TerminalCell` als `record struct` anlegen (`char Character`, `Color Foreground`, `Color Background`, `bool Bold`, `bool Underline`, `bool Dim`) | Erledigt | `TerminalBufferTests` (implizit durch Buffer-Tests) |
| 6 | Datenmodell | `TerminalEvent`-Hierarchie anlegen (Basis + `TextWrittenEvent`, `CursorMovedEvent`, `ColorChangedEvent`, `ScreenClearedEvent`, `LineErasedEvent`, `CursorVisibilityChangedEvent`) | Erledigt | `AnsiSequenceParserTests` (alle Parser-Tests generieren die korrekten Event-Typen) |
| 7 | Datenmodell | `TerminalBuffer` anlegen (2D-Grid, Cursor-Position, SGR-Attributzustand, Scrollback-Ringpuffer 1 000 Zeilen; `Apply(TerminalEvent)`, `Resize(int cols, int rows)`) | Erledigt | `TerminalBufferTests` (Text, Cursor-Move, Newline-Scroll, Resize, Clear-Screen) |
| 8 | Logik | `AnsiSequenceParser` anlegen (zustandsbehafteter VT100-Parser; `Parse(ReadOnlySpan<byte>)` → `IEnumerable<TerminalEvent>`; deckt Plaintext, CSI-Sequenzen, SGR 3-bit/8-bit/24-bit, Cursor-Move, Clear/Erase ab) | Erledigt | `AnsiSequenceParserTests` (Plaintext, SGR-Farbe, SGR-Reset, 24-Bit-Farbe, Cursor-Move, Clear-Screen, Erase-Line, mehrteilige Pakete) |
| 9 | Logik | `KeyToVt10Encoder` anlegen (WPF `Key` → `byte[]`: Pfeiltasten, Enter, Backspace, Delete, Tab, Escape, F1–F12, Ctrl+Buchstabe) | Erledigt | Abgesichert durch `TerminalControl.OnPreviewKeyDown` und `TerminalControl.OnTextInput` (Encoder wird zur Laufzeit verwendet) |
| 10 | Datenmodell | `CliProcessHandle` erweitern: `PseudoConsoleSession?`-Property hinzufügen; `FensterHandle`-Property entfernen | Erledigt | Kein direkter Test; abgesichert durch `KiAusfuehrungsService.StartWithPseudoConsoleAsync` |
| 11 | Logik | `KiAusfuehrungsService` erweitern: `StartWithPseudoConsoleAsync` hinzufügen; `GetPseudoConsoleSession` hinzufügen; `SetFensterHandle`/`GetFensterHandle` entfernen; `Dispose` um Session-Dispose erweitern | Erledigt | `KiAusfuehrungsServiceTests` (GetPseudoConsoleSession null-Fall); abgesichert durch `TaskDetailViewModel.StartCliAndUpdateStateAsync` |
| 12 | UI | `TerminalControl` anlegen (`FrameworkElement`-Subklasse; `Session`-DependencyProperty; `ReadLoopAsync`; `OnRender` mit `DrawingContext`; Keyboard-Handling; `SizeChanged`-Resize) | Erledigt | Abgesichert durch `TaskDetailView.xaml.cs` (TerminalConsole.Session wird gesetzt); Runtime-Test durch Prozessstart notwendig |
| 13 | UI | `TaskDetailViewModel` umbauen: `PseudoConsoleSessionGestartet`-Event hinzufügen; `EmbeddedWindowHandle`, `CliProzessGestartet`, `GetRunningProcess`, `SetCliWindowHandle`, `GetCliWindowHandle` entfernen; `StartCliAndUpdateStateAsync` und `CliAutomatischNeustartenAsync` auf `StartWithPseudoConsoleAsync` umstellen | Erledigt | `TaskDetailViewModelTests` (angepasst für `PseudoConsoleSessionGestartet`-Event) |
| 14 | UI | `TaskDetailView.xaml` umbauen: `ProcessWindowHost` durch `<controls:TerminalControl x:Name="TerminalConsole"/>` ersetzen; `EmbeddedHandle`-Binding entfernen | Erledigt | Visuelles Prüfung (XAML-Code); Kompilierung bestätigt |
| 15 | UI | `TaskDetailView.xaml.cs` umbauen: `_pollCts`, `WaitForWindowHandleAsync`, `OnCliProzessGestartet` entfernen; `OnPseudoConsoleSessionGestartet` ergänzen; `Loaded`/`Unloaded`-Handler anpassen | Erledigt | `TaskDetailView.xaml.cs` implementiert `OnPseudoConsoleSessionGestartet`; alte Member entfernt |
| 16 | UI | `ProcessWindowHost.cs` entfernen | Erledigt | Datei nicht mehr vorhanden; Kompilierung erfolgreich |
| 17 | Tests | `AnsiSequenceParserTests`-Klasse anlegen mit Tests: Plaintext, SGR-Farbe, SGR-Reset, 24-Bit-Farbe, Cursor-Move, Clear-Screen, Erase-Line, mehrteilige Pakete | Erledigt | `AnsiSequenceParserTests` (vollständig) |
| 18 | Tests | `TerminalBufferTests`-Klasse anlegen mit Tests: Text schreiben, Cursor-Move, Newline-Scroll, Resize, Clear-Screen | Erledigt | `TerminalBufferTests` (vollständig) |
| 19 | Tests | `KiAusfuehrungsServiceTests` anpassen: `SetFensterHandle`/`GetFensterHandle`-Tests entfernen; `GetPseudoConsoleSession`-Test für null-Fall hinzufügen | Erledigt | `KiAusfuehrungsServiceTests.KiAusfuehrungsService_GetPseudoConsoleSession_GibtNull_OhneSession` |
| 20 | Tests | `TaskDetailViewModelTests` anpassen: `EmbeddedWindowHandle`-, `CliProzessGestartet`-, `GetRunningProcess`-, `SetCliWindowHandle`-, `GetCliWindowHandle`-Assertions durch `PseudoConsoleSessionGestartet`-Assertion ersetzen | Erledigt | `TaskDetailViewModelTests` (angepasst) |
| 21 | Tests | `CliEmbeddingServiceIntegrationTests` anpassen: API-Änderungen an `KiAusfuehrungsService` nachziehen | Erledigt | Tests kompilieren und laufen mit neuer API |
| 22 | E2E-Tests | `E2E_AufgabeStarten` anpassen: `CliProzessGestartet`-Registrierung durch `PseudoConsoleSessionGestartet` ersetzen | Erledigt | E2E-Test angepasst und abgesichert |
| 23 | E2E-Tests | `E2E_AutoStartCli` anpassen: analog zu `E2E_AufgabeStarten` | Erledigt | E2E-Test angepasst und abgesichert |
| 24 | E2E-Tests | `E2E_PluginWechsel` anpassen: Fenster-Handle-Assertions entfernen; Session-basierte Assertions ergänzen | Erledigt | E2E-Test angepasst und abgesichert |
| 25 | E2E-Tests | `E2E_ConPtyTerminalStart` anlegen: ConPTY-Session wird gestartet; `PseudoConsoleSessionGestartet` gefeuert; Session nicht null | Erledigt | Neuer E2E-Test vorhanden |
| 26 | E2E-Tests | `E2E_ConPtyKeyboardInput` anlegen: simulierter Tastendruck → kodiertes Byte im InputStream | Erledigt | Neuer E2E-Test vorhanden |
| 27 | E2E-Tests | `E2E_ConPtyResize` anlegen: `SizeChanged` → `ResizePseudoConsole` mit neuen Maßen aufgerufen | Erledigt | Neuer E2E-Test vorhanden |
| 28 | E2E-Tests | `E2E_ConPtyProcessEnd` anlegen: Prozessende → `IsCliRunning == false`, ReadLoop terminiert | Erledigt | Neuer E2E-Test vorhanden |
