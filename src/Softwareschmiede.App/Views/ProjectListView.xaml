<UserControl x:Class="Softwareschmiede.App.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Projektliste links -->
        <Border Grid.Column="0"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Projekte"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="16,16,16,8" />

                <Button Grid.Row="1"
                        Content="+ Neues Projekt"
                        Command="{Binding ZeigeErstellungsFormularCommand}"
                        Margin="8,0,8,8"
                        Padding="8,6"
                        Background="{DynamicResource AccentBrush}"
                        Foreground="White"
                        BorderThickness="0"
                        HorizontalAlignment="Stretch" />

                <ListBox Grid.Row="2"
                         ItemsSource="{Binding Projekte}"
                         SelectedItem="{Binding SelectedProjekt}"
                         Background="Transparent"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="8,4">
                                <TextBlock Text="{Binding Name}"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource PrimaryTextBrush}" />
                                <TextBlock Text="{Binding Beschreibung}"
                                           Foreground="{DynamicResource SecondaryTextBrush}"
                                           FontSize="12"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Fehler -->
                <TextBlock Grid.Row="3"
                           Text="{Binding FehlerMeldung}"
                           Foreground="{DynamicResource ErrorBrush}"
                           Margin="8"
                           TextWrapping="Wrap"
                           Visibility="{Binding FehlerMeldung, Converter={StaticResource NullOrEmptyToVisibilityConverter}}" />
            </Grid>
        </Border>

        <!-- Erstellungsformular -->
        <Border Grid.Column="0"
                Grid.ColumnSpan="2"
                Background="{DynamicResource SurfaceBrush}"
                Panel.ZIndex="10"
                Visibility="{Binding IsCreateFormVisible, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Margin="32" MaxWidth="400" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Neues Projekt" FontSize="20" FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,16" />

                <TextBlock Text="Name" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4" />
                <TextBox Text="{Binding NeuerProjektName, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,12"
                         Padding="8,6"
                         BorderBrush="{DynamicResource BorderBrush}" />

                <TextBlock Text="Beschreibung (optional)" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4" />
                <TextBox Text="{Binding NeuerProjektBeschreibung, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,0,20"
                         Padding="8,6"
                         Height="80"
                         AcceptsReturn="True"
                         BorderBrush="{DynamicResource BorderBrush}" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Abbrechen"
                            Command="{Binding ZeigeErstellungsFormularCommand}"
                            Margin="0,0,8,0"
                            Padding="12,6"
                            Background="Transparent"
                            BorderBrush="{DynamicResource BorderBrush}"
                            Foreground="{DynamicResource PrimaryTextBrush}" />
                    <Button Content="Erstellen"
                            Command="{Binding ProjektErstellenCommand}"
                            Padding="12,6"
                            Background="{DynamicResource AccentBrush}"
                            Foreground="White"
                            BorderThickness="0" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Projektdetail rechts -->
        <ContentControl Grid.Column="1"
                        Content="{Binding DetailViewModel}" />
    </Grid>
</UserControl>
