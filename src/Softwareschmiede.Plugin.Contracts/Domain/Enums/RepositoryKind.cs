namespace Softwareschmiede.Domain.Enums;

/// <summary>Klassifikation des Repository-Typs für UI-Aktionsmatrixentscheidungen.</summary>
public enum RepositoryKind
{
    Unknown = 0,
    LocalDirectory = 1,
    RemoteGit = 2
}
